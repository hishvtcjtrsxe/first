name: to_do_app  # Match folder name exactly
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1  # Add this line
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
  # Comment out or remove assets section if not needed
  # assets:
  #   - assets/